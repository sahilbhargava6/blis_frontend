import React from "react";

export default function AdminReports() {
  return (
    <div>
      <h1 className="page-title">Global Reports</h1>
      <p className="page-subtitle">Visualize platform-wide statistics and conversion rates.</p>
      <div className="glass-panel" style={{ minHeight: "400px", display: "flex", alignItems: "center", justifyContent: "center", color: "var(--text-muted)" }}>
        [ Reporting Graphs Component ]
      </div>
    </div>
  );
}
