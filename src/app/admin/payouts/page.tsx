import React from "react";

export default function AdminPayouts() {
  return (
    <div>
      <h1 className="page-title">Pending Payouts</h1>
      <p className="page-subtitle">Approve bulk payouts and manage ledger balances.</p>
      <div className="glass-panel" style={{ minHeight: "400px", display: "flex", alignItems: "center", justifyContent: "center", color: "var(--text-muted)" }}>
        [ Payouts Table Component ]
      </div>
    </div>
  );
}
