import React from "react";

export default function AdminCampaigns() {
  return (
    <div>
      <h1 className="page-title">Campaigns</h1>
      <p className="page-subtitle">Manage master affiliate campaigns and configure commission splits.</p>
      <div className="glass-panel" style={{ minHeight: "400px", display: "flex", alignItems: "center", justifyContent: "center", color: "var(--text-muted)" }}>
        [ Campaigns Data Table Component ]
      </div>
    </div>
  );
}
